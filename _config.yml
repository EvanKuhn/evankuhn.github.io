# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: Evan's Tech Blog
author: Evan Kuhn
email:
description: On software engineering
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://evankuhn.github.io"
twitter_username: jekyllrb
github_username:  jekyll
github:
  is_project_page: false

# Build settings
# Post dates and URLs use this time zone, so local and GitHub builds match
timezone: America/Los_Angeles
markdown: kramdown
# plugins_dir: _plugins
# plugins: [jekyll-feed, jekyll-seo-tag, jekyll-tidy ]
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-tidy
  - jekyll-redirect-from

# The feed is XML with CDATA sections, which htmlbeautifier can't parse
jekyll_tidy:
  exclude: ["feed.xml"]

defaults:
  - scope:
      path: ""
    values:
      image: /favicon.png

# A hack to set the publisher
facebook:
 publisher: Evan Kuhn

# Exclude from processing.
# Setting this list replaces Jekyll's defaults, so they're repeated here.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - AGENTS.md
  - CLAUDE.md
